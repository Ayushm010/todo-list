export function createTodoList(title,description,priority,notes,checklist) {
    return {
        title: title,
        description: description,
        description: description,
        priority: priority,
        notes: notes,
        checklist:checklist,
    }
}
